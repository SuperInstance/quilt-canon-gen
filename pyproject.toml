[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "quilt-canon-gen"
version = "0.1.0"
description = "Creative canon generation + verification"
readme = "README.md"
requires-python = ">=3.8"

[tool.setuptools]
packages = ["quilt_canon_gen"]
